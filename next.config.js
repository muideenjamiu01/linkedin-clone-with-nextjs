/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.citypng.com", "static-exp1.licdn.com", "rb.gy"],
  },
};

module.exports = nextConfig;
